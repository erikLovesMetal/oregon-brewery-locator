this will be the map
<div ng-controller="IndexCtrl">
  <h1>{{ title }}</h1>
  <div class="row" ng-repeat="post in data.posts">
      <h2>{{ post.title }}</h2>
      <p>{{ post.contents }}</p>
  </div>
  {{centerPoint.lat }}
  {{centerPoint.lng }}
  <leaflet center="centerPoint" layers="layers" markers="markers"></leaflet>
</div>
